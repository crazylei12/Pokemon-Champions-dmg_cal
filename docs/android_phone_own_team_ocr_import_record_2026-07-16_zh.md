# Android 手机相册队伍 OCR 导入与问题记录（2026-07-16）

## 1. 任务与结论

本次在真实手机上使用已安装的 Pokémon Champions Assistant APK，把系统相册 `Screenshots` 相册中的 16 张队伍配置截图按两张一组识别并保存，共得到 8 支队伍。

- 设备：RMX3820，ADB 序列号 `6465e08`
- 应用：`com.crazylei12.pokemonchampionsassistant`
- 版本：`1.0.1 (4)`
- APK 类型：release，`debuggable=false`
- 截图范围：相册第 `1-12`、`21-24` 张
- 配对规则：奇数张是能力值页，偶数张是招式/道具页；实际识别时先录入偶数张，再录入对应奇数张
- 最终结果：8 支队伍均已保存在手机中，首页逐卡检查均显示“可用于计算”，没有未带满 4 个招式的提醒

最终队伍名称如下：

1. `Debug01-02`
2. `Debug03-04`
3. `Debug05-06`
4. `Debug07-08`
5. `Debug09-10`
6. `Debug11-12`
7. `Debug21-22`
8. `Debug23-24`

## 2. 相册配对与最终阵容

| 相册截图 | 保存名称 | 最终识别阵容 | 最终状态 |
| --- | --- | --- | --- |
| 1、2 | `Debug01-02` | 龙头地鼠 / 火暴兽 / 仙子伊布 / 雷丘 / 长毛巨魔 / 巨金怪 | 无缺项提醒 |
| 3、4 | `Debug03-04` | 雷丘 / 摔角鹰人 / 火暴兽 / 玛力露丽 / 仙子伊布 / 巨金怪 | 无缺项提醒 |
| 5、6 | `Debug05-06` | 奇麒麟 / 大狃拉 / 长毛巨魔 / 烈咬陆鲨 / 雷丘 / 大嘴鸥 | 人工修正后无缺项提醒 |
| 7、8 | `Debug07-08` | 风妖精 / 烈咬陆鲨 / 玛力露丽 / 雷丘 / 仙子伊布 / 巨金怪 | 人工修正后无缺项提醒 |
| 9、10 | `Debug09-10` | 火暴兽 / 摔角鹰人 / 雷丘 / 玛力露丽 / 皮可西 / 大嘴娃 | 无缺项提醒 |
| 11、12 | `Debug11-12` | 火暴兽 / 大嘴鸥 / 老翁龙 / 仙子伊布 / 雷丘 / 化石翼龙 | 人工确认后无缺项提醒 |
| 21、22 | `Debug21-22` | 大嘴鸥 / 烈咬陆鲨 / 玛力露丽 / 雷丘 / 皮可西 / 大钢蛇 | 人工补齐并保存后无缺项提醒 |
| 23、24 | `Debug23-24` | 火暴兽 / 风妖精 / 老翁龙 / 长毛巨魔 / 大钢蛇 / 耿鬼 | 无缺项提醒 |

## 3. OCR 漏项、冲突和误识别明细

下表中的“漏项”是字段为空或招式数量不足；“误识别”是字段有值，但与截图原值不一致。后者不会总是被当前待处理计数发现，因此必须人工对照原图。

| 队伍 | 宝可梦 | 类型 | OCR 结果 | 原图正确值 | 处理结果 |
| --- | --- | --- | --- | --- | --- |
| `Debug05-06` | 烈咬陆鲨 | 能力值漏项 | 生命为空 | `183` | 补为 `183` |
| `Debug05-06` | 烈咬陆鲨 | 能力值漏项 | 特攻为空 | `100` | 补为 `100` |
| `Debug05-06` | 烈咬陆鲨 | 能力值误识别 | 攻击 `183` | `182` | 改为 `182` |
| `Debug05-06` | 雷丘 | 能力值误识别 | 特攻 `96` | `99` | 改为 `99` |
| `Debug05-06` | 大嘴鸥 | 两页冲突 | 两页宝可梦结果冲突 | 大嘴鸥 | 点击“确认当前为大嘴鸥” |
| `Debug07-08` | 烈咬陆鲨 | 能力值漏项 | 生命为空 | `183` | 补为 `183` |
| `Debug07-08` | 烈咬陆鲨 | 能力值误识别 | 攻击 `183` | `182` | 改为 `182` |
| `Debug07-08` | 烈咬陆鲨 | 能力值误识别 | 特攻 `40` | `100` | 改为 `100` |
| `Debug11-12` | 大嘴鸥 | 两页冲突 | 两页宝可梦结果冲突 | 大嘴鸥 | 点击“确认当前为大嘴鸥” |
| `Debug21-22` | 烈咬陆鲨 | 能力值漏项 | 生命为空 | `183` | 补为 `183` |
| `Debug21-22` | 烈咬陆鲨 | 能力值误识别 | 攻击 `183` | `182` | 改为 `182` |
| `Debug21-22` | 烈咬陆鲨 | 能力值误识别 | 特攻 `40` | `100` | 改为 `100` |
| `Debug21-22` | 大嘴鸥 | 两页冲突 | 两页宝可梦结果冲突 | 大嘴鸥 | 点击“确认当前为大嘴鸥” |
| `Debug21-22` | 大钢蛇 | 招式漏项 | 只有地震、扑击、隐形岩 | 第 4 招为重磅冲撞 | 手动添加“重磅冲撞”，再点击“保存本次调整” |

没有发现需要人工补项的队伍：

- `Debug01-02`
- `Debug03-04`
- `Debug09-10`
- `Debug23-24`

### 3.1 根因定位（2026-07-16 修复）

根因、代码改动和回归验证的独立技术说明见 [Android 我方队伍 OCR 问题根因与修复说明](android_own_team_ocr_issue_root_cause_and_fix_2026-07-16_zh.md)。

本节表格记录的是修复前的手机结果。逐项回放原图、对比电脑端识别结果并在 RMX3820 上记录 Android OCR 中间候选后，确认并不是原图模糊或名称/招式目录缺数据，而是 Android 端的多处处理逻辑叠加：

1. **卡片上沿被宝可梦模型图拉偏。** 烈咬陆鲨等模型图与紫色卡片颜色区域连在一起时，连通域会把模型图也算入卡片。以第 21 张图为例，同一行左卡检测框为 `top=336, height=222`，右侧烈咬陆鲨却是 `top=311, height=246`；两张卡底边相同，右卡上沿被向上拉了约 25 像素。能力值格子按错误卡高计算后整体上移，直接造成生命、特攻漏读以及相邻行串读。现在每行两张卡会先按共同紫色面板的上、下边界归一化，再切能力值格子。
2. **能力值候选用“取最大值”合并。** 同一个数字的多种缩放/二值化结果发生分歧时，旧逻辑使用最大值，因而会稳定地把 `182` 偏向 `183`。同时，单字符乘十和 `11 → 111` 的猜值规则会把不完整 OCR 继续放大成看似合法的错误值。现在改用专门的拉丁数字识别器，精确裁剪优先、宽裁剪兜底，并按多候选一致性选择，不再对单字符擅自补零。
3. **部分中文名按目录顺序消歧。** `大嘴` 之类的残缺结果可以同时命中大嘴鸥和大嘴娃，旧逻辑会从同分候选中直接选一个，导致两页物种冲突。现在低置信度同分候选会保持未决，只有明显领先者才会落盘；完整本地化名称仍可直接采用。目录中 `Lightning Rod` 与兼容拼写 `Ligtning Rod` 都映射到“避雷针”，因此完整精确命中会优先采用首个正式条目，避免正确文字被误判为冲突。
4. **第一招裁剪包含属性图标。** 大钢蛇第一招的 OCR 原始结果曾是 `0重磅冲中撞`，目录无法匹配。现在第一招在常规区域失败或置信度不足时，会重试排除左侧属性图标的纯文字区域；真机重试结果为完整的 `重磅冲撞`。
5. **雷丘的 `6/9` 纠错只检查了十位。** 第 05 张原图的雷丘特攻是 `99`，ML Kit 稳定给出 `96`。旧的形态纠错只在两位数的十位为 `6` 或 `9` 时检查孔洞位置，因此首位 `9` 通过后，个位误读的 `6` 从未被校验。对原始个位数字连通域测得孔洞纵向中心比例约为 `0.292`，明显属于上半部的 `9`。现在会逐位提取数字连通域并分别校验 `6/9`，`96` 因而修正为 `99`；三位数中的同类误读也走同一逻辑。
6. **整卡兜底值没有经过同一套视觉校验。** 修正雷丘后首次真机回放还暴露出奇麒麟特防 `90—0` 被整卡 OCR 拼成 `900`：精确裁剪没有返回可用文本时，旧整卡兜底会绕过数字位数和 `6/9` 校验。现在整卡候选也必须用对应能力值格子的真实数字连通域校正；画面只有两位数字时，`900` 会按两位可见字形收敛为 `90`，校正后仍超出合法范围的值才会被丢弃。

### 3.2 修复后验证

- 4 组问题截图共 8 张在电脑端回放均为 `168/168`，确认原图内容、名称目录和招式目录完整。
- 新增能力值候选、物种歧义和第一招纯文字重试的回归测试；Android 单元测试全部通过。
- 在 RMX3820 上用原始第 21、22 张截图重新走正式悬浮 OCR：直接进入“识别完成、命名队伍”界面，招式/道具和能力值均为 `42/42`，没有再进入人工修正页。
- 真机中烈咬陆鲨六项能力值为 `183 / 182 / 103 / 100 / 150 / 124`；大嘴鸥两页一致；第 21、22 张中的雷丘“避雷针”和特攻 `99` 可正确保留；大钢蛇四招包含“重磅冲撞”。这次验证没有覆盖第 05 张中触发个位 `6/9` 误读的雷丘，不能据此推断第 05 张也已修好。
- 第 05、06 张在逐位 `6/9` 修正后再次用正式 APK 真机导入并导出待命名 JSON：雷丘六项为 `137 / 142 / 75 / 99 / 100 / 178`，其中 `actualStats.spa=99`；无需再手动把 `96` 改成 `99`。
- 本次复测只验证导入结果，没有再保存一支重复队伍；手机首页原有 8 支队伍保持不变。

## 4. 识别流程中遇到的产品问题

### 4.1 修正页的完成状态与完整度计数不一致

`Debug21-22` 在修正页一度显示“待处理 0 只”，但顶部仍是“招式/道具 41/42、能力值 41/42”。页面允许直接保存，回到首页后才出现“1 只宝可梦未带满 4 个招式”的提醒。

最终定位到大钢蛇缺少“重磅冲撞”。这说明当前“待处理数量归零”不等价于“所有字段完整”，保存前必须同时检查两个 `42/42` 计数，并在首页再次检查提醒。

建议：

- 当任一完整度计数小于 `42/42` 时，不应仅显示“待处理 0 只”。
- 保存按钮应明确提示剩余字段，或要求用户确认仍要保存不完整队伍。
- 首页能够发现的招式数量问题，也应在修正页直接定位到具体槽位。

### 4.2 能力值有值但数值错误时不会被稳定标记（本轮已补齐逐位视觉校验）

烈咬陆鲨多次出现固定模式：生命为空、攻击把 `182` 识别成 `183`、特攻把 `100` 识别成 `40`。其中只有空字段会稳定进入待处理列表，`183` 和 `40` 这种“看似合法”的数值需要人工对照截图才能发现。

本次已经修复造成这些固定误读的卡片边界和候选合并逻辑。作为第二道保护，仍建议为能力值增加规则校验或低置信度提示，不要只把空字段当成待处理项。

### 4.3 大嘴鸥反复出现两页物种冲突（已修复）

在 `Debug05-06`、`Debug11-12`、`Debug21-22` 中，大嘴鸥都曾触发“两页识别到的宝可梦不一致”，但当前候选和原图均可确认是大嘴鸥。低置信度同分候选不再按目录顺序强行选中；第 21、22 张真机复测中两页已经稳定合并为大嘴鸥。上述三组截图继续作为固定回归样本。

### 4.4 修正页会沿用上一支队伍名称

保存 `Debug05-06` 后，下一次进入修正页时名称框仍带着 `Debug05-06`。如果不主动清空，会造成重名或错误命名。

建议每次新建 OCR 草稿时重置名称，或者清楚标注这是上一支队伍的名称。

### 4.5 手动调整页必须滚到底部另行保存

在大钢蛇中添加“重磅冲撞”后，列表立即显示 4 招，但离开页面前如果没有继续向下滚动并点击“保存本次调整”，改动不会持久化。强制重启应用后，第 4 招会再次消失。

建议：

- 有未保存改动时，返回或离开页面应弹出确认。
- 保存按钮可固定在底部，不要只放在长页面最末端。
- 招式选择完成后可显示明显的“尚未保存”状态。

### 4.6 命名弹窗的自动输入和保存不稳定

使用原搜狗中文输入法执行自动文本输入时，`Debug01-02` 被输入成错误中文文本 `D额不过00号`。切换到 OEM 搜狗英文子类型后，英文名称才能稳定输入。

命名弹窗中直接点击“保存队伍”也曾没有响应。稳定做法是：

1. 输入名称并检查输入框文本；
2. 收起软键盘；
3. 连续发送两次焦点切换；
4. 按确认键保存；
5. 检查是否已经返回相册。

### 4.7 修正浮窗的无障碍树不完整

修正浮窗为 Compose/悬浮窗口时，无障碍转储经常只能看到后面的相册窗口，无法可靠取得当前修正字段。现场只能以设备截图和固定坐标核对，增加了自动化难度。

建议补齐浮窗语义节点、可访问名称和可点击范围，便于测试与无障碍使用。

## 5. 本次操作中的人为失误及恢复

本节记录自动化执行本身发生的错误，避免与 OCR 产品问题混淆。

### 5.1 调试截图一度污染相册顺序

最初把一张调试截图写到 `/sdcard/codex-assistant-home.png`，它被 MediaStore 当作最新图片，导致相册编号整体偏移。发现后立即删除文件和对应媒体记录，之后所有设备截图只写到 `/data/local/tmp`，不再污染用户相册。

### 5.2 首支队伍名称曾被输入法改写

`Debug01-02` 第一次被输入为 `D额不过00号`。该错误在保存前通过输入框检查发现，没有进入最终队伍；随后切换英文输入子类型并重新输入正确名称。

### 5.3 清空沿用名称时曾发生拼接

准备把沿用的 `Debug05-06` 改成 `Debug07-08` 时，第一次全选没有生效，结果变成 `Debug07-08Debug05-06`。该错误在保存前发现；随后把光标移到末尾并逐字符删除，再输入正确名称。

### 5.4 数字键盘坐标变化曾产生 `1829`

修正烈咬陆鲨攻击值时，软键盘弹出后页面坐标发生变化，一次把值写成了 `1829`。该值未保存，随后采用“每次只编辑一个字段、收起键盘、截图确认”的方式改回 `182`。

### 5.5 竖屏坐标曾按截图分辨率直接点击

竖屏截图渲染为 `918x2048`，设备物理坐标实际为 `1240x2772`。第一次进入 `Debug21-22` 手动调整时没有换算，点击没有命中按钮。确认物理分辨率后按比例换算坐标，成功进入页面。

### 5.6 第一次添加“重磅冲撞”后忘记保存

第一次添加后只确认列表出现第 4 招便返回首页，没有继续滚动到底部点击“保存本次调整”。强制重启后首页提醒仍存在，重新进入页面确认改动没有持久化。第二次重新添加并明确点击保存，随后再次强制重启，首页提醒消失。

### 5.7 临时测试包触发了 ColorOS 安装风险验证

排查早期曾短暂安装无桌面入口的 instrumentation/fixture 测试包，ColorOS 因此显示了安装风险验证界面。发现后立即卸载两个临时包，后续真机验证全部改为覆盖安装原正式包 `com.crazylei12.pokemonchampionsassistant`，不再创建隐藏测试应用。最终检查只有这一个相关包，状态为 `hidden=false`，桌面入口为 `.MainActivity`，release 签名证书 SHA-256 为 `671B45190A9DAC81A2747355CB9F10703503F1302EAF3E59582A282DD827EEF8`。

## 6. 最终验证与手机收尾状态

完成后执行了以下验证：

- 强制停止并重新启动 APK，确认队伍数据不是仅存在于当前页面内存。
- 首页显示“我的队伍（8）”。
- 逐卡滚动检查 8 支队伍名称和 6 人阵容。
- 8 支队伍都显示“可用于计算”。
- `Debug21-22` 的未满 4 招提醒在持久化保存后消失。
- OCR 修复版覆盖安装后再次使用第 21、22 张原图，直接进入“保存自己的队伍”命名弹窗；没有产生人工修正页，也没有保存重复队伍。
- 悬浮识别服务已停止。
- MediaProjection 屏幕采集会话已停止。
- 原输入法已恢复为 `com.sohu.inputmethod.sogou/.SogouIME`。
- 原输入法子类型已恢复为 `312418808`。
- 屏幕旋转已恢复为自动模式 `free`。

本次设备证据保存在本机忽略目录：

`D:\crazylei12\pokemon-champions-assistant\.tmp\goal-team-import-20260716`

关键证据包括各组 OCR 结果、修正前后截图、命名弹窗无障碍树、最终首页 8 队截图、`Debug21-22` 大钢蛇 3 招/4 招对比和重启后的最终验证截图。

本次根因修复的最终真机命名弹窗证据为：

`D:\crazylei12\pokemon-champions-assistant\.tmp\final-21-22-result.png`

## 7. 八组重新落盘复测（2026-07-16 本轮）

在 RMX3820（设备序列号 `6465e08`）上重新运行正式 APK `1.0.1 (4)`，按“招式/道具页在前、能力值页在后”的顺序，重新导入相册第 `1-12`、`21-24` 张截图并保存为 8 支队伍：

- `Debug01-02`
- `Debug03-04`
- `Debug05-06`
- `Debug07-08`
- `Debug09-10`
- `Debug11-12`
- `Debug21-22`
- `Debug23-24`

八组双图 OCR 都直接进入“保存自己的队伍”命名弹窗，没有进入缺项或跨页冲突修正页。命名时 OEM 输入法会进入横屏全屏输入界面；稳定做法是输入队名、收起键盘、从 APK 无障碍树确认输入框文本，再点击“保存队伍”。最终首页显示“我的队伍（8）”，逐卡检查的 8 个队名和 6 人阵容均与原图配对一致。

本轮发现 1 个“字段有值但数值错误”的漏网项：

| 队伍 | 宝可梦 | 字段 | OCR 结果 | 原图正确值 | 手机内处理 |
| --- | --- | --- | --- | --- | --- |
| `Debug05-06` | 雷丘 | 特攻 | `96` | `99` | 手动改为 `99`，点击“保存本次调整”，重新打开确认已经持久化 |

本轮没有发现字段缺项。旧故障热点复核结果如下：

- `Debug05-06` 烈咬陆鲨六项能力值为 `183 / 182 / 103 / 100 / 150 / 124`。
- `Debug07-08` 烈咬陆鲨六项能力值为 `183 / 182 / 103 / 100 / 150 / 124`。
- `Debug21-22` 烈咬陆鲨六项能力值为 `183 / 182 / 103 / 100 / 150 / 124`。
- `Debug05-06`、`Debug11-12`、`Debug21-22` 的大嘴鸥均未再触发跨页物种冲突。
- `Debug21-22` 大钢蛇四招完整，第一招为“重磅冲撞”，其余为“地震 / 扑击 / 隐形岩”。

本轮证据保存在：

`D:\crazylei12\pokemon-champions-assistant\.tmp\goal-team-import-20260716`

其中包括 8 组命名状态截图、最终首页截图、`Debug05-06` 雷丘特攻修正后截图、`Debug07-08` 烈咬陆鲨能力值截图，以及 `Debug21-22` 大钢蛇四招截图。

### 7.1 雷丘特攻 `96 → 99` 的根因补修

上方八组重新落盘表格记录的是当时仍然存在的真实漏网项，也证明此前把第 21、22 张的验证结论推广到第 05、06 张是不成立的。根因不在截图清晰度，也不在通用候选投票，而在 `OwnTeamRecognition.kt` 的数字形态纠错只处理两位数的十位，完全没有检查个位。

本轮修复把能力值数字按从左到右的连通域逐位校验。第 05 张雷丘特攻两个数字的孔洞中心比例分别约为 `0.289` 和 `0.292`，都符合 `9` 的上孔特征，因此 ML Kit 的 `96` 会稳定修正为 `99`。同时，整卡 OCR 兜底也接入同一格子的视觉位数校验，避免 `90—0` 被当成三位数 `900`。

验证结果：

- 新增回归用例覆盖 `96 + [0.289, 0.292] → 99`、三位数中的 `6/9`、歧义孔洞保持原值，以及 `900 + 2 个可见数字 → 90`。
- Android 单元测试和双 ABI `release` 构建通过；输出仍为独立的 `arm64-v8a` 与 `x86_64` APK。
- RMX3820 首次从空白状态复跑能力值页时曾非确定性地得到 `41/42`；导出草稿确认唯一缺项是奇麒麟特防。该格整卡 OCR 把画面中的 `90—0` 拼成 `900`，新合法范围检查将 `900` 丢弃后形成缺项，说明整卡兜底仍需接入真实数字位数校验。接入后同图复跑恢复为 `42/42`。
- 最终 APK 在 RMX3820 上重新完成第 05、06 张双图导入：能力值页 `42/42`，招式/道具页 `42/42`，待命名 JSON 为 `DAMAGE_READY`、零警告。六只能力值依次为奇麒麟 `195 / 99 / 122 / 178 / 90 / 82`、大狃拉 `155 / 200 / 80 / 54 / 102 / 172`、长毛巨魔 `202 / 189 / 85 / 103 / 97 / 80`、烈咬陆鲨 `183 / 182 / 103 / 100 / 150 / 124`、雷丘 `137 / 142 / 75 / 99 / 100 / 178`、大嘴鸥 `135 / 63 / 167 / 147 / 92 / 85`。其中雷丘特攻稳定为 `99`，奇麒麟特防为 `90`；临时结果随后删除，原有 8 支队伍未被替换。
