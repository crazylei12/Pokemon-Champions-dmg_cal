let verificationMode: string = 'verify';

export function setStage4VerificationMode(mode: string): void {
  verificationMode = mode;
}

export function getStage4VerificationMode(): string {
  return verificationMode;
}
